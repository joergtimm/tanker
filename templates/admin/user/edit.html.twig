{% extends 'admin/base.html.twig' %}

{% block title %}Benutzer bearbeiten{% endblock %}

{% block admin_content %}
<div class="max-w-4xl mx-auto bg-white shadow-md rounded-lg p-8">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold text-gray-800">Benutzer bearbeiten</h1>
        <a href="{{ path('admin_user_index') }}" class="text-gray-500 hover:text-gray-700">Zurück zur Liste</a>
    </div>

    {{ include('admin/user/_form.html.twig', {'button_label': 'Speichern'}) }}

    <div class="mt-8 pt-6 border-t border-gray-100 flex justify-between">
        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
