<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}NIG Tanker{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="text-slate-900 font-sans antialiased">
        <nav class="bg-white/80 backdrop-blur-md sticky top-0 z-50 border-b border-slate-200/60 no-print" {% if app.request.attributes.get('_route') starts with 'admin_' %}style="display: none;"{% endif %} x-data="{ mobileMenuOpen: false }">
            <div class="container mx-auto px-4 max-w-7xl">
                <div class="flex justify-between h-20">
                    <div class="flex items-center">
                        <a href="{{ path('app_home') }}" class="flex items-center gap-2 group">
                            <div class="w-10 h-10 bg-blue-600 rounded-xl flex items-center justify-center shadow-lg shadow-blue-200 group-hover:scale-105 transition-transform">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                                </svg>
                            </div>
                            <span class="text-xl font-black text-slate-900 tracking-tight uppercase">Nig Tanker</span>
                        </a>
                    </div>

                    <!-- Desktop Menu -->
                    <div class="hidden md:flex items-center gap-2">
                        <a href="{{ path('app_schema') }}" class="px-4 py-2 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-all">Schema</a>
                        {% if app.user %}
                            {% if is_granted('ROLE_USER') %}
                                <div class="relative" x-data="{ open: false }" @click.outside="open = false" @keydown.escape.window="open = false">
                                    <button type="button" @click="open = !open" class="px-4 py-2 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-all inline-flex items-center gap-2">
                                        Spiele
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                                            <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 011.08 1.04l-4.25 4.25a.75.75 0 01-1.06 0L5.21 8.27a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
                                        </svg>
                                    </button>
                                    <div x-show="open" x-transition class="absolute left-0 mt-2 w-44 rounded-lg border border-slate-200 bg-white shadow-xl overflow-hidden" style="display: none;">
                                        <a href="{{ path('app_for_row') }}" class="block px-4 py-2 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 transition-all">4 gewinnt</a>
                                        <a href="{{ path('app_cube') }}" class="block px-4 py-2 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 transition-all">Cube</a>
                                    </div>
                                </div>
                            {% endif %}
                            <a href="{{ path('app_favorites') }}" class="px-4 py-2 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-all">Favoriten</a>
                            <a href="{{ path('app_profile') }}" class="px-4 py-2 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-lg transition-all">Profil</a>
                            {% if is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('admin_dashboard') }}" class="px-4 py-2 text-sm font-semibold text-amber-600 hover:text-amber-700 hover:bg-amber-50 rounded-lg transition-all">Admin</a>
                            {% endif %}
                            <div class="h-8 w-[1px] bg-slate-200 mx-2"></div>
                            <div class="flex items-center gap-3 pl-2">
                                <span class="text-sm font-medium text-slate-500">{{ app.user.userIdentifier }}</span>
                                <a href="{{ path('app_logout') }}" class="px-4 py-2 text-sm font-bold text-red-600 hover:bg-red-50 rounded-lg transition-all">Abmelden</a>
                            </div>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="ml-4 px-6 py-2.5 bg-slate-900 text-white rounded-xl text-sm font-bold hover:bg-blue-600 transition-all shadow-lg shadow-slate-200 active:scale-95">Anmelden</a>
                        {% endif %}
                    </div>

                    <!-- Mobile Menu Button -->
                    <div class="flex md:hidden items-center">
                        <button @click="mobileMenuOpen = !mobileMenuOpen" class="text-gray-500 hover:text-blue-600 focus:outline-none">
                            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path x-show="!mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                                <path x-show="mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" style="display: none;" />
                            </svg>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Mobile Menu -->
            <div x-show="mobileMenuOpen"
                 class="md:hidden bg-white border-b border-slate-200 pb-6 px-4 space-y-1 shadow-xl"
                 style="display: none;">
                <a href="{{ path('app_schema') }}" class="block px-4 py-3 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">Schema</a>
                {% if app.user %}
                    {% if is_granted('ROLE_USER') %}
                        <div class="px-4 pt-3 text-[10px] font-bold text-slate-400 uppercase tracking-widest">Spiele</div>
                        <a href="{{ path('app_for_row') }}" class="block px-4 py-3 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">4 gewinnt</a>
                        <a href="{{ path('app_cube') }}" class="block px-4 py-3 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">Cube</a>
                    {% endif %}
                    <a href="{{ path('app_favorites') }}" class="block px-4 py-3 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">Favoriten</a>
                    <a href="{{ path('app_profile') }}" class="block px-4 py-3 text-sm font-semibold text-slate-600 hover:text-blue-600 hover:bg-blue-50 rounded-xl transition-all">Profil</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('admin_dashboard') }}" class="block px-4 py-3 text-sm font-semibold text-amber-600 hover:text-amber-700 hover:bg-amber-50 rounded-xl transition-all">Admin</a>
                    {% endif %}
                    <div class="px-4 py-4 mt-2 border-t border-slate-100">
                        <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest block mb-1">Eingeloggt als</span>
                        <span class="text-sm font-semibold text-slate-700">{{ app.user.userIdentifier }}</span>
                    </div>
                    <a href="{{ path('app_logout') }}" class="block px-4 py-3 text-sm font-bold text-red-600 hover:bg-red-50 rounded-xl transition-all">Abmelden</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="block w-full text-center px-4 py-4 bg-slate-900 text-white rounded-2xl text-sm font-bold hover:bg-blue-600 transition-all shadow-lg">Anmelden</a>
                {% endif %}
            </div>
        </nav>
        <main class="min-h-screen">
            {% block body %}{% endblock %}
        </main>

        <footer class="bg-white border-t border-slate-200 pt-16 pb-8 no-print mt-20">
            <div class="container mx-auto px-4 max-w-7xl">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-12 mb-16">
                    <div class="col-span-1 md:col-span-1">
                        <a href="{{ path('app_home') }}" class="flex items-center gap-2 mb-6">
                            <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center shadow-md">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                                </svg>
                            </div>
                            <span class="text-lg font-black text-slate-900 tracking-tight uppercase">Nig Tanker</span>
                        </a>
                        <p class="text-slate-500 text-sm leading-relaxed max-w-xs">
                            Finden Sie die günstigsten Tankstellen in Ihrer Nähe. Echtzeit-Preise und einfache Navigation.
                        </p>
                    </div>
                    <div>
                        <h4 class="text-xs font-bold text-slate-900 uppercase tracking-widest mb-6">Navigation</h4>
                        <ul class="space-y-4">
                            <li><a href="{{ path('app_home') }}" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Startseite</a></li>
                            <li><a href="{{ path('app_schema') }}" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Schema</a></li>
                            {% if app.user %}
                                <li><a href="{{ path('app_for_row') }}" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">4 gewinnt</a></li>
                                <li><a href="{{ path('app_favorites') }}" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Favoriten</a></li>
                                <li><a href="{{ path('app_profile') }}" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Profil</a></li>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li><a href="{{ path('admin_dashboard') }}" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Admin</a></li>
                                {% endif %}
                            {% endif %}
                        </ul>
                    </div>
                    <div>
                        <h4 class="text-xs font-bold text-slate-900 uppercase tracking-widest mb-6">Rechtliches</h4>
                        <ul class="space-y-4">
                            <li><a href="#" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Datenschutz</a></li>
                            <li><a href="#" class="text-sm text-slate-500 hover:text-blue-600 transition-colors">Impressum</a></li>
                        </ul>
                    </div>
                </div>
                <div class="pt-8 border-t border-slate-100 flex flex-col md:flex-row justify-between items-center gap-4">
                    <p class="text-xs text-slate-400">
                        &copy; {{ "now"|date("Y") }} Nig Tanker. Alle Rechte vorbehalten.
                    </p>
                    <div class="flex gap-6">
                        <span class="text-[10px] font-bold text-slate-300 uppercase tracking-[0.2em]">Crafted with passion</span>
                    </div>
                </div>
            </div>
        </footer>
        {{ include('_cookie_banner.html.twig') }}
    </body>
</html>
